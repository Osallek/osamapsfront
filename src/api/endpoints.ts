const endpoints = {
  data: {
    area: () => `/data/area.json`,
    birth: () => `/data/birth.json`,
    birthPerCapita: () => `/data/birth-per-capita.json`,
    common: () => `/data/common.json`,
    death: () => `/data/death.json`,
    deathPerCapita: () => `/data/death-per-capita.json`,
    density: () => `/data/density.json`,
    pop: () => `/data/pop.json`,
  },
};

export default endpoints;
